import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    paddingBottom: 32,
  },
  switch: {
    position: 'absolute',
    alignItems: 'center',
    bottom: 0,
    right: 0,
    flexDirection: 'row',
  },
});
